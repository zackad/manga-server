module.exports = {
  theme: {
    extend: {
      maxHeight: (theme) => theme('spacing'),
      minHeight: (theme) => theme('spacing'),
      minWidth: (theme) => theme('spacing'),
    },
  },
  variants: {},
  plugins: [],
}
